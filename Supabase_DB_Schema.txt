create table public.contributions (
  id bigint generated by default as identity not null,
  group_id bigint null,
  user_id uuid null,
  amount double precision null,
  round integer null,
  status text null default 'pending'::text,
  due_date timestamp with time zone null,
  paid_at timestamp with time zone null,
  recipient_id uuid null,
  constraint contributions_pkey primary key (id),
  constraint contributions_group_id_fkey foreign KEY (group_id) references groups (id) on delete CASCADE,
  constraint contributions_recipient_id_fkey foreign KEY (recipient_id) references auth.users (id),
  constraint contributions_user_id_fkey foreign KEY (user_id) references profiles (id) on delete CASCADE
) TABLESPACE pg_default;


create table public.group_chat (
  id bigint generated by default as identity not null,
  group_id bigint null,
  user_id uuid null,
  user_name text null,
  message text null,
  timestamp timestamp with time zone not null default timezone ('utc'::text, now()),
  constraint group_chat_pkey primary key (id),
  constraint group_chat_group_id_fkey foreign KEY (group_id) references groups (id) on delete CASCADE,
  constraint group_chat_user_id_fkey foreign KEY (user_id) references profiles (id)
) TABLESPACE pg_default;


create table public.group_members (
  id bigint generated by default as identity not null,
  group_id bigint null,
  user_id uuid null,
  user_name text null,
  joined_at timestamp with time zone not null default timezone ('utc'::text, now()),
  paid_contributions integer null default 0,
  received_payouts integer null default 0,
  rotation_order integer null,
  constraint group_members_pkey primary key (id),
  constraint group_members_group_id_user_id_key unique (group_id, user_id),
  constraint group_members_group_id_fkey foreign KEY (group_id) references groups (id) on delete CASCADE,
  constraint group_members_user_id_fkey foreign KEY (user_id) references profiles (id) on delete CASCADE
) TABLESPACE pg_default;

create trigger tr_update_group_member_count
after INSERT
or DELETE on group_members for EACH row
execute FUNCTION update_group_member_count ();


create table public.groups (
  id bigint generated by default as identity not null,
  name text not null,
  description text null,
  total_pot double precision null default 0.0,
  contribution double precision null default 0.0,
  frequency text null,
  max_members integer null,
  current_members integer null default 0,
  next_payout_date timestamp with time zone null,
  created_by uuid null,
  join_code text null,
  status text null default 'active'::text,
  group_status text null default 'pending'::text,
  current_round integer null default 0,
  created_at timestamp with time zone not null default timezone ('utc'::text, now()),
  constraint groups_pkey primary key (id),
  constraint groups_join_code_key unique (join_code),
  constraint groups_created_by_fkey foreign KEY (created_by) references profiles (id)
) TABLESPACE pg_default;


create table public.payment_proofs (
  id bigint generated by default as identity not null,
  contribution_id bigint null,
  group_id bigint null,
  sender_id uuid null,
  sender_name text null,
  recipient_id uuid null,
  recipient_name text null,
  round integer null,
  gcash_name text null,
  gcash_number text null,
  transaction_no text null,
  screenshot_url text null,
  amount double precision null,
  status text null default 'pending'::text,
  submitted_at timestamp with time zone not null default timezone ('utc'::text, now()),
  verified_at timestamp with time zone null,
  verified_by_id uuid null,
  rejection_reason text null,
  screenshot_path text null,
  constraint payment_proofs_pkey primary key (id),
  constraint payment_proofs_contribution_id_fkey foreign KEY (contribution_id) references contributions (id) on delete CASCADE,
  constraint payment_proofs_group_id_fkey foreign KEY (group_id) references groups (id) on delete CASCADE,
  constraint payment_proofs_recipient_id_fkey foreign KEY (recipient_id) references profiles (id),
  constraint payment_proofs_sender_id_fkey foreign KEY (sender_id) references profiles (id),
  constraint payment_proofs_verified_by_id_fkey foreign KEY (verified_by_id) references profiles (id)
) TABLESPACE pg_default;


create table public.profiles (
  id uuid not null,
  full_name text not null,
  address text null,
  age integer null,
  email text null,
  gcash_name text null,
  gcash_number text null,
  profile_picture text null,
  urcode_path text null,
  created_at timestamp with time zone not null default timezone ('utc'::text, now()),
  id_front_path text null,
  id_back_path text null,
  constraint profiles_pkey primary key (id),
  constraint profiles_email_key unique (email),
  constraint profiles_id_fkey foreign KEY (id) references auth.users (id) on delete CASCADE
) TABLESPACE pg_default;


create table public.round_rotations (
  id bigint generated by default as identity not null,
  group_id bigint null,
  round integer null,
  payout_date timestamp with time zone null,
  recipient_id uuid null,
  recipient_name text null,
  status text null default 'pending'::text,
  completed_at timestamp with time zone null,
  total_collected double precision null,
  constraint round_rotations_pkey primary key (id),
  constraint round_rotations_group_id_round_key unique (group_id, round),
  constraint round_rotations_group_id_fkey foreign KEY (group_id) references groups (id) on delete CASCADE,
  constraint round_rotations_recipient_id_fkey foreign KEY (recipient_id) references profiles (id)
) TABLESPACE pg_default;


create table public.transactions (
  id bigint generated by default as identity not null,
  group_id bigint null,
  user_id uuid null,
  type text null,
  amount double precision null,
  round integer null,
  date timestamp with time zone not null default timezone ('utc'::text, now()),
  description text null,
  constraint transactions_pkey primary key (id),
  constraint transactions_group_id_fkey foreign KEY (group_id) references groups (id) on delete CASCADE,
  constraint transactions_user_id_fkey foreign KEY (user_id) references profiles (id)
) TABLESPACE pg_default;


create table public.notifications (
  id bigint generated by default as identity not null,
  user_id uuid not null,
  actor_user_id uuid null,
  group_id bigint null,
  payment_proof_id bigint null,
  contribution_id bigint null,
  round integer null,
  type text not null,
  title text not null,
  message text not null,
  is_read boolean not null default false,
  created_at timestamp with time zone not null default timezone ('utc'::text, now()),
  read_at timestamp with time zone null,
  metadata jsonb null,
  constraint notifications_pkey primary key (id),
  constraint notifications_actor_user_id_fkey foreign KEY (actor_user_id) references profiles (id) on delete set null,
  constraint notifications_contribution_id_fkey foreign KEY (contribution_id) references contributions (id) on delete CASCADE,
  constraint notifications_group_id_fkey foreign KEY (group_id) references groups (id) on delete CASCADE,
  constraint notifications_payment_proof_id_fkey foreign KEY (payment_proof_id) references payment_proofs (id) on delete CASCADE,
  constraint notifications_user_id_fkey foreign KEY (user_id) references profiles (id) on delete CASCADE
) TABLESPACE pg_default;

create index IF not exists notifications_user_created_idx on public.notifications using btree (user_id, created_at desc) TABLESPACE pg_default;
create index IF not exists notifications_user_unread_idx on public.notifications using btree (user_id, is_read) TABLESPACE pg_default;
create index IF not exists notifications_payment_proof_idx on public.notifications using btree (payment_proof_id) TABLESPACE pg_default;
create unique INDEX IF not exists notifications_unique_payment_pending_idx on public.notifications using btree (user_id, payment_proof_id, type) TABLESPACE pg_default
where  (payment_proof_id is not null);


create or replace function public.create_payment_proof_notification()
returns trigger
language plpgsql
security definer;

create trigger tr_create_payment_proof_notification
after INSERT
or
update on public.payment_proofs for EACH row
execute FUNCTION create_payment_proof_notification ();
